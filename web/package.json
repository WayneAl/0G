{
  "name": "@0x402/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-pitch.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-pitch.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@0x402/seal": "workspace:*",
    "@0x402/storage": "workspace:*",
    "viem": "^2.23.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^8.0.0",
    "vitest": "^3.0.5"
  }
}
